import React from 'react';
import s from './Footer.module.css'

const Footer = () => {
    return (
        <div className={s.footers}>
            This is footer
        </div>
    );
};

export default Footer;